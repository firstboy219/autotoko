<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Android 13+. Asked for at runtime; refusing it costs the reminder and
         nothing else, so nothing in the app blocks on it. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- APK ini dibagikan di luar Play Store, jadi pembaruannya harus bisa
         dipasang dari dalam aplikasi sendiri. Android tetap meminta
         persetujuan per aplikasi sebelum pemasang dibuka. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Scanner">

        <activity

            android:name=".DashboardActivity"

            android:exported="false"

            android:label="Dashboard"

            android:screenOrientation="portrait" />

        <activity

            android:name=".PayoutActivity"

            android:exported="false"

            android:label="Pencairan Dana"

            android:screenOrientation="portrait" />

        <activity

            android:name=".PayoutBatchActivity"

            android:exported="false"

            android:label="Batch Pencairan"

            android:screenOrientation="portrait" />

        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:label="Versi Aplikasi"
            android:screenOrientation="portrait" />

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".DeliveryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".TextScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".StockActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PhotoActivity"
            android:exported="false"
            android:parentActivityName=".HistoryActivity" />

        <activity
            android:name=".PendingActivity"
            android:exported="false"
            android:parentActivityName=".ScanActivity" />


        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <!-- Pemasang Android tidak bisa membaca berkas milik aplikasi lain;
             APK yang baru diunduh dipinjamkan lewat sini. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.berkas"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
